# The 7 files of ornith-ai/Ornith-1.5-9B-MLX-4bit: 5.06 GB.
#
# Naming the files is what lets core/fetch.sh resume a dropped shard by byte
# range. The weights arrive in one shard, so a link that resets is expensive.
#
# assets/ is excluded on purpose. It holds a logo and an eval chart, which the
# engine never reads.
#
# One path per line. Blank lines and # lines are skipped. No globs.
README.md
chat_template.jinja
config.json
model.safetensors
model.safetensors.index.json
tokenizer.json
tokenizer_config.json
